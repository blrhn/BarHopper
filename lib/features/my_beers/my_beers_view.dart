import 'package:flutter/material.dart';

class MyBeersView extends StatelessWidget {
  const MyBeersView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Text("My Beers"));
  }
}
